import React from 'react';

import {View} from '../utils/wrapper/nativewind';

const CartAction = () => {
  return (
    <View className="flex-1 bg-black">
      <Text>CartActions</Text>
    </View>
  );
};

export default CartAction;
